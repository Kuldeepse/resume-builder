import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export', // 🚀 CRITICAL: Forces Next.js 16 to compile the app into 100% static HTML/CSS files
  images: {
    unoptimized: true, // Required by Next.js when utilizing static export pipelines
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  eslint: {
    ignoreDuringBuilds: true,
  }
};

export default nextConfig;
